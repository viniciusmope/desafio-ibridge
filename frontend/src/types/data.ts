export interface ResumoItem {
  campanha_nome: string;
  lista_nome: string;
  total_chamadas: string; 
  sem_contato: string;
  contato: string;
  abordagem: string;
  fechamento: string;
}

export interface TopItem {
  nome: string;
  total_fechamentos: string; 
}